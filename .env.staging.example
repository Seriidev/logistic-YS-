# ============================================================
# FRONTEND STAGING — copy to .env.staging
# Deploy: docker compose --project-name yuusell-frontend-staging --env-file .env.staging up -d --build
# ============================================================

ENV=staging
FRONTEND_PORT=3032
VITE_API_URL=https://api.staging.yuusell.com/api/v1
