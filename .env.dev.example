# ============================================================
# FRONTEND DEV — copy to .env.dev
# Deploy: docker compose --project-name yuusell-frontend-dev --env-file .env.dev up -d --build
# ============================================================

ENV=dev
FRONTEND_PORT=3022
VITE_API_URL=https://api.dev.yuusell.com/api/v1
