# ============================================================
# FRONTEND PRODUCTION — copy to .env.prod
# Deploy: docker compose --project-name yuusell-frontend-prod --env-file .env.prod up -d --build
# ============================================================

ENV=prod
FRONTEND_PORT=3002
VITE_API_URL=https://api.yuusell.com/api/v1
